$kind: http-request
name: Bajar un documento
url: "{{baseUrl}}/solicitudes/{{solicitudId}}/documentos/{{documentoId}}/descargar"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3064358992829323
